#!/bin/bash
# {{ ansible_managed }}

export ALLOWED_HOSTS='{{ item.allowed_hosts[env_name]|join(',') }}'

export DB_NAME='{{ item.name }}'
export DB_USERNAME='{{ item.name }}'
export DB_HOST='localhost'
export DB_PASSWORD='{{ vault.dittodemo.db_password }}'

export DJANGO_SETTINGS_MODULE='{{ item.django_settings_module[env_name] }}'

export SECRET_KEY='{{ vault.dittodemo.django_secret_key }}'

